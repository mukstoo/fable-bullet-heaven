{
  "name": "gravehorde",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "GRAVEHORDE — a bullet-heaven / survivors-like built with Phaser 3 + TypeScript + Vite",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "phaser": "3.90.0"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vite": "^7.1.0"
  }
}
